{
  "version": "1.0.0",
  "name": "prism.rhapsody.view.dashboard",
  "private": true,
  "scripts": {
    "postinstall": "npm run server:install && npm run client:install",
    "client:install": "cd client-v && npm install",
    "server:install": "cd server && npm install",
    "start": "node server/startup",
    "build": "cd client-v && npm run build",
    "debug": "cd client-v && npm run dev",
    "mock": "cd client-v && npm run mock",
    "test": "cd client-v && npm test"
  },
  "devDependencies": {}
}
